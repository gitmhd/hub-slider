// Create instance of HUBSlider
let doc = new HUBSlider();

// Initialize the slider
doc.init("doc");

// Set options to slider
doc.options({
   slidingPrev: "hub-bottom",
   slidingNext: "hub-top",
   autoSlide: false,
   animateSlide: false,
   arrowsRemoveStyle: "both",
});
